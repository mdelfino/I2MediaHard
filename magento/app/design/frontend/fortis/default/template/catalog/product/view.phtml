<?php
/**
 * Magento
 *
 * NOTICE OF LICENSE
 *
 * This source file is subject to the Academic Free License (AFL 3.0)
 * that is bundled with this package in the file LICENSE_AFL.txt.
 * It is also available through the world-wide-web at this URL:
 * http://opensource.org/licenses/afl-3.0.php
 * If you did not receive a copy of the license and are unable to
 * obtain it through the world-wide-web, please send an email
 * to license@magentocommerce.com so we can send you a copy immediately.
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade Magento to newer
 * versions in the future. If you wish to customize Magento for your
 * needs please refer to http://www.magentocommerce.com for more information.
 *
 * @category    design
 * @package     base_default
 * @copyright   Copyright (c) 2011 Magento Inc. (http://www.magentocommerce.com)
 * @license     http://opensource.org/licenses/afl-3.0.php  Academic Free License (AFL 3.0)
 */

/**
 * Product view template
 *
 * @see Mage_Catalog_Block_Product_View
 * @see Mage_Review_Block_Product_View
 */
?>
<?php $_helper = $this->helper('catalog/output'); ?>
<?php $_product = $this->getProduct(); ?>
<script type="text/javascript">
    var optionsPrice = new Product.OptionsPrice(<?php echo $this->getJsonConfig() ?>);
</script>
<div id="messages_product_view"><?php echo $this->getMessagesBlock()->getGroupedHtml() ?></div>
<div class="product-view">
    <?php //my:del <div class="product-essential"> ?>
    <form action="<?php echo $this->getSubmitUrl($_product) ?>" method="post" id="product_addtocart_form"<?php if($_product->getOptions()): ?> enctype="multipart/form-data"<?php endif; ?>>
        <div class="no-display">
            <input type="hidden" name="product" value="<?php echo $_product->getId() ?>" />
            <input type="hidden" name="related_product" id="related-products-field" value="" />
        </div>
        
        <div class="product-top-wrapper">
			<div class="product-top">
				<div class="product-top-inner clearer">
				
					<?php if ($rightBoxEnabled = Mage::getStoreConfig('themeadmin/products/product_right_box_enabled', Mage::app()->getStore()->getId())): ?>
                    <div class="product-right-box">

						<?php //Display brand logo, if enabled ****************************************************************** ?>
                        <?php if(Mage::getStoreConfig('themeadmin/attributes/brand_product_view_enabled', Mage::app()->getStore()->getId())): ?>
							<?php $manufAttrId = Mage::getStoreConfig('themeadmin/attributes/brand_attr_id', Mage::app()->getStore()->getId()); ?>
                            <?php if ($_product->getData($manufAttrId)): //If manufacturer/brand attribute exists ?>
                                <?php
								$manufImageExt = trim(Mage::getStoreConfig('themeadmin/attributes/brand_image_extension', Mage::app()->getStore()->getId()));
								$manufPageBasePath = trim(Mage::getStoreConfig('themeadmin/attributes/brand_page_base_path', Mage::app()->getStore()->getId())); //Base path to each brand's page
								$manufLinkSearch = Mage::getStoreConfig('themeadmin/attributes/brand_link_search_enabled', Mage::app()->getStore()->getId()); //If option is true, brand logo is linking to Quick Search results
								$attr = $_product->getResource()->getAttribute($manufAttrId);	//Attr. object
								$manufValue = trim($attr->getFrontend()->getValue($_product));	//Attr. value
								$manufValueCode = str_replace(" ", "_", strtolower($manufValue));	//Attr. value (simplified)
								$manufLabel = $attr->getStoreLabel();							//Attr. label
								//$manufLabelCode = str_replace(" ", "-", strtolower($manufLabel)); //Attr. label (simplified)
								$manufImageUrl = Mage::getBaseUrl(Mage_Core_Model_Store::URL_TYPE_MEDIA) . 'infortis/attributes/' . $manufAttrId . '/' . $manufValueCode . '.' . $manufImageExt;

								if ($manufLinkSearch)
									$manufPageUrl = Mage::getUrl() . 'catalogsearch/result/?q=' . $manufValue;
								elseif ($manufPageBasePath != '')
								{
									//If $manufPageBasePath is '/', then it has to be omitted
									//Change brand name to lowercase, and replace spaces with hyphens
									$basePath = ($manufPageBasePath == '/') ? '' : $manufPageBasePath . '/';
									$manufPageUrl = Mage::getUrl() . $basePath . str_replace(" ", "-", strtolower($manufValue));
								}
								else
									$manufPageUrl = '';

								//if (file_exists($manufImageUrl) == FALSE)
								//	$manufImageUrl = $this->getSkinUrl("images/img/brand-placeholder.png");
								?>
                                <div class="item box-brand">
                                	<?php if ($manufPageUrl != ''): ?>
                                    	<a href="<?php echo $manufPageUrl;?>" title="<?php echo str_replace("%s", $manufValue, $this->__('Click to see more products from %s')); ?>"><img src="<?php echo $manufImageUrl;?>" alt="<?php echo $manufValue; ?>" width="210" height="50" /></a>
                                    <?php else: ?>
                                    	<img src="<?php echo $manufImageUrl;?>" title="<?php echo $manufLabel .': '. $manufValue; ?>" alt="<?php echo $manufValue; ?>" width="210" height="50" />
                                    <?php endif; ?>
                                </div>
                            <?php endif; ?>
                        <?php endif; //****************************************************************************************** ?>
                        
						<?php if ($tmp_html = $this->getChildHtml('i_block_product_right_bottom')): ?><?php //Predefined CMS block ?>
                        	<div class="product-right-bottom-wrapper"><?php echo $tmp_html; ?></div>
                        <?php endif; ?>

                    </div> <!-- end: product-right-box -->
                    <?php endif; ?>
                    
                    <?php
                    	//Count image box width and "product-shop" width based on image width
						$newImgWidth = trim(Mage::getStoreConfig('themeadmin/images/product_main_width', Mage::app()->getStore()->getId()));
						$defImgWidth = 265;					//Default image width
						$imgBoxWidth = $defImgWidth + 12;	//Default image box ("product-img-box" class) width
						$centerBoxWidth = 397;				//Default central box ("product-shop" class) width
						if ($rightBoxEnabled == false) $centerBoxWidth += 240; //If no right box, add its width to central box width
						if ($newImgWidth > 0)
						{
							$diff = $newImgWidth - $defImgWidth;
							$imgBoxWidth += $diff;
							$centerBoxWidth -= $diff;
						}
					?>
                    
                    <div class="product-shop"<?php if($newImgWidth) echo " style=\"width:{$centerBoxWidth}px;\"";?>>

                        <div class="product-name">
                            <h1><?php echo $_helper->productAttribute($_product, $_product->getName(), 'name') ?></h1>
                        </div>
                        
                        <?php echo $this->getReviewsSummaryHtml($_product, false, true); //reviews ?>
                        
                        <?php if ($_product->getShortDescription()): ?>
                            <div class="short-description">
                                <div class="std"><?php echo $_helper->productAttribute($_product, nl2br($_product->getShortDescription()), 'short_description') ?></div>
                            </div>
                        <?php endif;?>
                        
                        <?php if ($extrahint_html = $this->getChildHtml('extrahint')): //qty increments ?>
                        	<div class="extrahint-wrapper"><?php echo $extrahint_html; ?></div>
						<?php endif; ?>
						
						<?php if ($_product->isAvailable()): ?>
							<?php if ($_product->isConfigurable()): ?>
								<div style="display:none;"><?php echo $this->getPriceHtml($_product); ?></div>
							<?php endif; ?>
						<?php endif; ?>
						
						<?php if ($type_html = trim($this->getChildHtml('product_type_data'))): //boundle, grouped, only X left ?>
							<div class="product-type-data<?php if($_product->isGrouped()) echo " is-grouped"; ?>"><?php echo $type_html; ?></div>
                        <?php endif; ?>

                        <?php echo $this->getTierPriceHtml(); //tier prices ?>
    
                        <?php if (!$this->hasOptions()): //add to cart when no options ?>
							<?php if($_product->isSaleable()): ?>
                                <div class="add-to-box">
                                    <?php echo $this->getPriceHtml($_product); ?>
                                    <?php echo $this->getChildHtml('addtocart') ?>
                                </div>
                            <?php endif; ?>
                            <?php echo $this->getChildHtml('extra_buttons') ?>
                        <?php endif; ?>

                        <?php if ($_product->isSaleable() && $this->hasOptions()): ?>
							<?php if ($container1_html = $this->getChildChildHtml('container1', '', true, true)): ?>
                            	<div class="container1-wrapper"><?php echo $container1_html; ?></div>
                            <?php endif; ?>
                        <?php endif;?>
                        
                        <?php if ($addto_box_html = $this->getChildHtml('addto')): //compare, wishlist ?>
                        	<div class="action-box clearer"><?php echo $addto_box_html; ?></div>
                        <?php endif; ?>
                        
                        <?php echo $this->getChildHtml('alert_urls'); //alerts ?>
                        <?php echo $this->getChildHtml('other'); ?>
                        
                        <?php if ($tmp_html = $this->getChildHtml('i_block_product_main_bottom')): ?> <?php //Predefined CMS block ?>
                        	<div class="product-main-bottom-wrapper"><?php echo $tmp_html; ?></div>
                        <?php endif; ?>

                    </div> <!-- end: product-shop -->

                    <div class="product-img-box"<?php if($newImgWidth) echo " style=\"width:{$imgBoxWidth}px;\"";?>>
						<?php echo $this->getChildHtml('media') ?>
						<?php // Product labels "new" and "sale"
                        	echo $this->getChildHtml('media_labels'); ?>
                    </div>
                </div> <!-- end: product-top-inner -->
			</div> <!-- end: product-top -->
		</div> <!-- end: product-top-wrapper -->

        <?php if ($_product->isSaleable() && $this->hasOptions()): ?>
			<?php if ($container2_html = $this->getChildChildHtml('container2', '', true, true)): ?>
        		<div class="container2-wrapper"><?php echo $container2_html; ?></div>
        	<?php endif; ?>
        <?php endif; ?>
        
        <?php // Related products
		$replaceRelated = Mage::getStoreConfig('themeadmin/products/replace_related', Mage::app()->getStore()->getId());
		if ($replaceRelated == 0) //never replace
		{
			echo '<div class="block-related-wrapper">' . $this->getChildHtml('catalog.product.related') . '</div>';
		}
		elseif ($replaceRelated == 1) //always replace
		{
			if ($replaceRelated_html = $this->getChildHtml('i_block_product_replace_related'))
				echo '<div class="block-related-wrapper"><div class="product-replace-related-wrapper">' . $replaceRelated_html . '</div></div>';
		}
		else //replace if no related ($replaceRelated == 2)
		{
			if ($blockRelated_html = $this->getChildHtml('catalog.product.related')) //related exists
				echo '<div class="block-related-wrapper">' . $blockRelated_html . '</div>';
			else //no related
				if ($replaceRelated_html = $this->getChildHtml('i_block_product_replace_related'))
					echo '<div class="block-related-wrapper"><div class="product-replace-related-wrapper">' . $replaceRelated_html . '</div></div>';
		}
		?>
        
    </form>
    <script type="text/javascript">
    //<![CDATA[
        var productAddToCartForm = new VarienForm('product_addtocart_form');
        productAddToCartForm.submit = function(button, url) {
            if (this.validator.validate()) {
                var form = this.form;
                var oldUrl = form.action;

                if (url) {
                   form.action = url;
                }
                var e = null;
                try {
                    this.form.submit();
                } catch (e) {
                }
                this.form.action = oldUrl;
                if (e) {
                    throw e;
                }

                if (button && button != 'undefined') {
                    button.disabled = true;
                }
            }
        }.bind(productAddToCartForm);

        productAddToCartForm.submitLight = function(button, url){
            if(this.validator) {
                var nv = Validation.methods;
                delete Validation.methods['required-entry'];
                delete Validation.methods['validate-one-required'];
                delete Validation.methods['validate-one-required-by-name'];
                if (this.validator.validate()) {
                    if (url) {
                        this.form.action = url;
                    }
                    this.form.submit();
                }
                Object.extend(Validation.methods, nv);
            }
        }.bind(productAddToCartForm);
    //]]>
    </script>
    <!-- here was the end: product-essential -->

	<div class="product-collateral">
    	<?php $info_tabs_html = $this->getChildHtml('info_tabs');
			if (trim($info_tabs_html)) echo $info_tabs_html; ?>

		<?php // Open the "Reviews" tab, when "X Review(s)" or "Be the first to review this product" links are clicked ?>
		<script type="text/javascript">
		jQuery(function() {
			jQuery("#goto-reviews, #goto-reviews-form").click(function() {
				jQuery("#product-tabs").data("tabs").click( jQuery("#tabreviews").index() );
			});
		});
		</script>

		<?php // This script creates tabs: product tabs, upsell products, related products ?>
		<script type="text/javascript">
        jQuery(function() {
            jQuery(".tabs").tabs(".tabs-panes .pane", {rotate: false});
        });
        </script>

        <?php
		// Up-sell products (replace with static CMS block, if enabled)
		$upsellEnabled = Mage::getStoreConfig('themeadmin/products/upsell_enabled', Mage::app()->getStore()->getId());
		$replaceUpsell = Mage::getStoreConfig('themeadmin/products/replace_upsell', Mage::app()->getStore()->getId());
        if ($upsellEnabled == 1) //upsell enabled
		{
			if ($replaceUpsell == 0) //never replace
			{
				echo $this->getChildHtml('upsell_products');
			}
			elseif ($replaceUpsell == 1) //always replace
			{
				if ($replaceUpsell_html = $this->getChildHtml('i_block_product_replace_upsell'))
						echo '<div class="product-replace-upsell-wrapper">' . $replaceUpsell_html . '</div>';
			}
			else //replace if no upsell ($replaceUpsell == 2)
			{
				if ($blockUpsell_html = $this->getChildHtml('upsell_products'))
					echo $blockUpsell_html;
				else
					if ($replaceUpsell_html = $this->getChildHtml('i_block_product_replace_upsell'))
						echo '<div class="product-replace-upsell-wrapper">' . $replaceUpsell_html . '</div>';
			}
		}
		else //upsell disabled
		{
			if ($replaceUpsell == 1 || $replaceUpsell == 2)
				if ($replaceUpsell_html = $this->getChildHtml('i_block_product_replace_upsell'))
					echo '<div class="product-replace-upsell-wrapper">' . $replaceUpsell_html . '</div>';
		}
		?>
        
        <?php echo $this->getChildHtml('product_additional_data') ?>
	</div>
</div>
